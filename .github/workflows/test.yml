name: Demo

on:
  workflow_dispatch
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
#   - uses: ./
    - name: "What branch am I on"
      run: echo "${{ github.repository }}"
